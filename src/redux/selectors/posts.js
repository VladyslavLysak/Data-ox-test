
export const getPosts = (state) => state.posts.items;

export const getSearch = (state) => state.posts.search;

export const getPostById = (state) => state.postPage.item;

export const getError = (state) => state.posts.error;
